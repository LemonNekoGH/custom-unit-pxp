{
  "name": "custom-unit-pxp-root",
  "private": true,
  "version": "0.0.3",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run build --filter=packages/*",
    "publish": "bun run publish --filter=packages/*"
  }
}
